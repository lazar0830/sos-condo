{
  "name": "s.o.s.-condo---ai-property-management",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy",
    "deploy:hosting": "npm run build && firebase deploy --only hosting",
    "deploy:functions": "cd functions && npm install && npm run build && cd .. && firebase deploy --only functions",
    "seed:categories": "node scripts/seed-component-categories.mjs"
  },
  "dependencies": {
    "@google/genai": "^1.27.0",
    "firebase": "^11.4.0",
    "firebase-admin": "^13.6.1",
    "i18next": "^25.8.4",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-i18next": "^16.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
